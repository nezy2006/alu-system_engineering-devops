nstall_nginx_web_server
# Installs and configures nginx to serve "Holberton School"

# Update package list and install nginx
apt-get update -y
apt-get install -y nginx

# Ensure Nginx is listening on port 80
# By default, Nginx config listens on 80, but we can overwrite default
DEFAULT_CONF="/etc/nginx/sites-available/default"

# Backup original config
cp $DEFAULT_CONF ${DEFAULT_CONF}.bak

# Replace root / index content with "Holberton School for the win!"
cat > /var/www/html/index.nginx-debian.html <<EOF
Holberton School for the win!
EOF

# Ensure Nginx service is started (use service command, not systemctl)
service nginx restart

